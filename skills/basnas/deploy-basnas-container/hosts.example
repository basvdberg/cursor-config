# Example Windows hosts entries for the local server (LAN / VPN).
# Copy the line below into C:\Windows\System32\drivers\etc\hosts (administrator).
# Zone and IP: local-server.env.example (LOCAL_SERVER_DNS_ZONE, LOCAL_SERVER_LAN_IP).

192.168.2.2 admin.basnas airflow.basnas immich.basnas
